/** Node CLI / deploy entry — uses absolute zk asset paths. */
export { VoidBallotAPI } from './index.js';
export {
  CompiledVoidBallotContract,
  ledger,
  pureCircuits,
  zkConfigPath,
} from '../../contracts/index.js';
